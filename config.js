window.DABSY_CONFIG = {
  // Leave empty until you connect your own Gemini key.
  // For a production deployment, use a backend/proxy and never commit a secret.
  GEMINI_API_KEY: "",
  // Current stable Flash model listed by Google's model docs at build time.
  GEMINI_MODEL: "gemini-3.8-flash",
  APP_NAME: "D.A.B.S.y",
  VERSION: "1.0.0-premium"
};
